uddi.enabled=true
verbose.enabled=true

uddi.url=${uddi.url}
ws.name=${ws.name}

# ws url below is used only if uddi is disabled above
ws.url=${ws.url}

# Restaurant IDs to use
rst.ws.name1 = ${rst.ws.name}1
rst.ws.name2 = ${rst.ws.name}2

